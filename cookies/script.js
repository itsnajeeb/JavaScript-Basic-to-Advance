//Cookies
    //A baked sweet (like chocolate chip cookies).
    // A web browser "cookie" (small piece of data stored on your computer by websites).
    // A playful expression (like calling someone a "smart cookie").

    // document.cookie = 'name=najeeb'
    // document.cookie = 'email=iamnajeebjung.com'




